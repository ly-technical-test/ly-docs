openapi: 3.1.0
info:
  title: ly-services docs
  version: 1.0.0
  description: Pontos de acesso do microsserviço ly-services.
servers:
  - url: http://localhost:3000
    description: Servidor Local
paths:
  /v1/ping:
    get:
      summary: Verificação do sistema
      description: Retorna mensagem de teste para verificar se a API está no ar.
      responses:
        '200':
          description: API funcionando normalmente
          content:
            application/json:
              example:
                code: 200
                data: pong
                message: ok
                metadata:
                  responseAt: '2026-08-28T00:00:00.000Z'
                  method: GET
                  route: /v1/ping
  /v1/auth/register:
    post:
      summary: Cadastro de usuário
      description: Cria uma nova conta de usuário no sistema.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - email
                - password
              properties:
                name:
                  type: string
                  example: Usuario Teste
                email:
                  type: string
                  format: email
                  example: usuario@email.com
                password:
                  type: string
                  example: senha12345
      responses:
        '201':
          description: Conta criada com sucesso
        '400':
          description: Dados informados estão incorretos
  /v1/auth/login:
    post:
      summary: Entrar na conta
      description: Autentica o usuário e libera o acesso ao sistema.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
                - password
              properties:
                email:
                  type: string
                  format: email
                  example: usuario@email.com
                password:
                  type: string
                  example: senha12345
      responses:
        '200':
          description: Login realizado com sucesso
        '401':
          description: E-mail ou senha incorretos
  /v1/billing/issue:
    post:
      summary: Gerar cobrança
      description: Cria uma nova cobrança no sistema
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - amount
                - description
                - payment_method
              properties:
                amount:
                  type: number
                  example: 100.50
                description:
                  type: string
                  example: Cobrança de teste
                payment_method:
                  type: string
                  enum:
                    - pix
                    - boleto
                    - cartao
                    - all
                  example: pix
                  description: Método de pagamento desejado (pix; boleto; cartao; ou 'all' para permitir todos os métodos)
      responses:
        '201':
          description: Cobrança gerada com sucesso
        '401':
          description: Acesso não autorizado
  /v1/billing/simulate/{id}:
    post:
      summary: Simular pagamento
      description: Simula o pagamento de uma cobrança existente
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          example: cob_123456
      responses:
        '200':
          description: Pagamento simulado com sucesso
        '404':
          description: Cobrança não encontrada
  /v1/billing/list:
    get:
      summary: Listar cobranças
      description: Mostra todas as cobranças registradas para o usuário
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Lista de cobranças obtida com sucesso
        '401':
          description: Acesso não autorizado
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
